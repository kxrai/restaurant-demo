// the file where we connect our react application to the dom

import React from "react";
import { ReactDOM } from "react";

import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));